//Máximo divisor comum: O máximo divisor comum (mdc) de dois inteiros a, b é o maior número inteiro que divide a e b.
var a = 10; // primeiro numero
var b = 20; //segundo numero
var i;     // for
function mdc(a, b){
    for (i = 0; )

    while (a % candidato != 0 || b % candidato != 0){
           candidato--;
    } 

    return candidato;
}

console.log(" O mdc dos numeros " +a+ " e " +b+ " é " + mdc(a,b))